version: "3.7"

x-logging:
  &default-logging
  options:
    max-size: '12m'
    max-file: '5'
  driver: json-file
x-deploy:
  &default-deploy
  replicas: 1
  labels:
    - "traefik.enable=false"
  update_config:
    parallelism: 1
    failure_action: rollback
    order: start-first
x-proxy-env:
  &proxy-env
  HTTP_PROXY: "http://wwwcache.gla.ac.uk:8080"
  HTTPS_PROXY: "http://wwwcache.gla.ac.uk:8080"
  NO_PROXY: "localhost,127.0.0.1,130.209.0.0/16,*.gla.ac.uk"


services:
  app:
    image: ${IMAGE_NAME}
    logging: *default-logging
    environment:
      <<: *proxy-env
      CONTAINER_ROLE: app
    build:
      context: .
    secrets:
      - source: dotenv
        target: .env
    depends_on:
      - redis
      - mysql
      - mailhog
    networks:
      - proxy
      - private
    expose:
      - "80"
    deploy:
      update_config:
        parallelism: 1
        failure_action: rollback
        order: start-first
      replicas: 1
      labels:
        - "traefik.enable=true"
        - "traefik.http.services.${TRAEFIK_BACKEND}.loadbalancer.server.port=80"
        - "traefik.http.routers.${TRAEFIK_BACKEND}-secured.rule=Host(`${TRAEFIK_HOSTNAME}`)"
        - "traefik.http.routers.${TRAEFIK_BACKEND}-secured.entrypoints=web-secured"
        - "traefik.http.routers.${TRAEFIK_BACKEND}-secured.tls=true"

  scheduler:
    image: ${IMAGE_NAME}
    logging: *default-logging
    deploy: *default-deploy
    environment:
      <<: *proxy-env
      CONTAINER_ROLE: scheduler
    depends_on:
      - app
    networks:
      - private
    secrets:
      - source: dotenv
        target: .env

  queue:
    image: ${IMAGE_NAME}
    logging: *default-logging
    deploy: *default-deploy
    stop_grace_period: 30s
    environment:
      <<: *proxy-env
      CONTAINER_ROLE: queue
    depends_on:
      - app
    networks:
      - private
    secrets:
      - source: dotenv
        target: .env

  migrations:
    image: ${IMAGE_NAME}
    logging: *default-logging
    deploy: *default-deploy
    networks:
      - private
    environment:
      <<: *proxy-env
      CONTAINER_ROLE: migrations
    depends_on:
      - app
    secrets:
      - source: dotenv
        target: .env

  redis:
    image: redis:5.0.5
    deploy: *default-deploy
    networks:
      - private
    volumes:
      - redis:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]

  mysql:
    image: mysql:5.7
    deploy: *default-deploy
    networks:
      - private
    volumes:
      - mysql:/var/lib/mysql
    environment:
      MYSQL_DATABASE: homestead
      MYSQL_ROOT_PASSWORD: root
      MYSQL_USER: homestead
      MYSQL_PASSWORD: secret

  mailhog:
    image: mailhog/mailhog
    deploy:
      labels:
        - "traefik.enable=true"
        - "traefik.http.services.mailhog-${TRAEFIK_BACKEND}.loadbalancer.server.port=8025"
        - "traefik.http.routers.mailhog-${TRAEFIK_BACKEND}-secured.rule=Host(`mail-${TRAEFIK_HOSTNAME}`)"
        - "traefik.http.routers.mailhog-${TRAEFIK_BACKEND}-secured.entrypoints=web-secured"
        - "traefik.http.routers.mailhog-${TRAEFIK_BACKEND}-secured.tls=true"
    expose:
      - "8025"
    networks:
      - proxy
      - private

volumes:
  redis:
    driver: "local"
  mysql:
    driver: "local"

networks:
  private:
  proxy:
    external: true

secrets:
  dotenv:
    external: true
    name: ${DOTENV_NAME}
